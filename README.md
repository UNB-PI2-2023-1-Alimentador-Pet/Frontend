# Frontend

# Rodar o projeto

0. Instalar as dependências:

```
yarn install
```

Se estiver utilizando o iOS, instale também as dependências do CocoaPods:

```
cd ios
pod install
cd ..
```

1. Instalar o app:

```
yarn android
```

ou

```
yarn ios
```

2. Rodar o app:

```
yarn start
```
